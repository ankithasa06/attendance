import { loadModels, getFaceDescriptor } from './dist/services/faceRecognition.js';
// Wait, faceRecognition isn't emitted as a separate file.
// Let's just import the entire index.js or write a standalone script.
